# Core LangChain - Updated for Python 3.13 compatibility
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchainhub>=0.1.21
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=3.0.0
langgraph-checkpoint-postgres>=2.0.0
langsmith>=0.2.0

# LLM Providers
openai>=1.50.0       # For OpenRouter API compatibility
langchain-groq>=0.2.0  # Groq fast-inference (primary provider)

# Vector Stores & Embeddings
psycopg[binary]>=3.1.0   # psycopg3 — async-ready Postgres driver
psycopg-pool>=3.0.0      # connection pool for pgvector adapter
pgvector>=0.3.0          # pgvector type adapters for psycopg3

# Document Processing
pypdf>=5.0.0
beautifulsoup4>=4.12.0
python-multipart>=0.0.6

# Web Tools
requests>=2.32.0
duckduckgo-search>=6.0.0
ddgs>=1.1.0

# API Framework (for later)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Notion Integration
notion-client==2.2.1

# LangChain classic (AgentExecutor)
langchain-classic>=0.1.0

# Utilities
python-dotenv>=1.0.0

